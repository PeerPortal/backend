---
alwaysApply: true
---

## 数据库管理与交互规范

数据库是应用的核心，正确的管理和交互方式对于数据一致性和系统稳定性至关重要。

### 1. 数据库源

-   **唯一可信源**: **线上 Supabase 数据库的结构是唯一可信的真实来源**。
-   **参考模式文件**: `scripts/database/db_schema.sql` 文件是数据库结构的参考，用于帮助理解表结构和关系。但不保证此文件与线上数据库完全同步。

### 2. 数据库变更流程

-   **禁止通过迁移文件变更**: 不要使用 ORM 迁移工具（如 Alembic）或手动编写 SQL 迁移文件来修改生产数据库。
-   **必须通过 Supabase 后台**: 所有对数据库表结构（如增删改字段、创建新表、修改索引）的变更，**必须直接在 Supabase 管理后台的图形化界面中进行操作**。
-   **更新参考文件**: 在后台完成变更后，应考虑（但非强制）将最新的 schema 导出并更新到 `scripts/database/db_schema.sql`，以便团队成员参考。

### 3. CRUD 层交互

-   **异步操作**: 所有数据库操作都必须是异步的，使用 `async`/`await` 语法。
-   **依赖注入**: 必须通过 `Depends(get_db_or_supabase)` 获取数据库连接，不能在函数内部手动创建连接。
-   **抽象兼容**: CRUD 函数需要设计为可以处理两种类型的数据库连接：`asyncpg` (PostgreSQL) 和 `supabase-py` (Supabase 客户端)。逻辑应尽可能保持通用。
-   **参数与返回**:
    -   CRUD 函数应接收 Pydantic `Schema` 对象作为数据输入参数。
    -   函数的返回值应该是从数据库查询出的原生数据（如 ORM 对象或字典），而不是 HTTP 响应。

### 4. 安全性

-   **无 RLS 策略**: 由于本项目不依赖 PostgreSQL 的行级别安全性（Row-Level Security），所有数据访问控制都在后端业务逻辑中实现。
-   **CRUD 层的数据过滤**: 业务逻辑层（CRUD）负责确保用户只能访问他们被授权的数据。
