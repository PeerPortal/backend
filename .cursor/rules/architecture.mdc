---
alwaysApply: true
---

## 分层架构

本项目的后端遵循清晰的三层架构模式，以确保代码的可维护性、可扩展性和关注点分离。

### 1. 路由层 (`app/api/routers/`)

-   **职责**:
    -   处理所有与 HTTP 相关的逻辑，包括请求的解析、路径参数、查询参数的提取。
    -   使用 `Depends` 进行依赖注入，获取数据库连接 (`get_db_or_supabase`) 和认证用户 (`get_current_user`)。
    -   调用 `CRUD` 层的函数来执行业务逻辑。
    -   格式化最终的 HTTP 响应，包括设置正确的状态码和返回 Pydantic 模型定义的 JSON 数据。
    -   处理特定于路由的异常。
    -   通过依赖注入实现权限控制，例如 `Depends(require_mentor_role())`。
    
    ```python
    from fastapi import APIRouter, Depends
    from app.api.deps import get_current_user, get_db_or_supabase, require_admin_role
    from app.schemas.user_schema import UserRead
    from app.schemas.item_schema import ItemCreate, ItemRead
    from app.crud import crud_item

    router = APIRouter()

    @router.post("/items/", response_model=ItemRead)
    async def create_item(
        *,
        db: dict = Depends(get_db_or_supabase),
        current_user: UserRead = Depends(get_current_user),
        item_in: ItemCreate
    ):
        """
        创建一个新物品，需要用户认证。
        """
        return crud_item.create_with_owner(db=db, obj_in=item_in, owner_id=current_user.id)

    @router.delete("/items/{item_id}", dependencies=[Depends(require_admin_role)])
    async def delete_item(
        *,
        db: dict = Depends(get_db_or_supabase),
        item_id: int
    ):
        """
        删除一个物品，仅限管理员。
        """
        # ... 删除逻辑 ...
        return {"message": "Item deleted successfully"}
    ```
-   **禁止**:
    -   直接进行数据库查询或操作。
    -   包含复杂的业务逻辑。
    -   手动创建依赖实例。

### 2. CRUD 层 (`app/crud/`)

-   **职责**:
    -   封装所有与数据库的交互逻辑（创建、读取、更新、删除）。
    -   接收由路由层传入的数据库会话/连接对象和经过验证的 Pydantic Schema 对象。
    -   执行数据库查询，并处理与数据相关的逻辑。
    -   返回从数据库中检索到的数据（可以是 ORM 模型实例或字典）。
    -   应同时兼容 `asyncpg` 和 `supabase-py` 两种数据库客户端。
-   **禁止**:
    -   处理 HTTP 请求或响应对象。
    -   包含特定于 API 端点的逻辑。

### 3. Schema 层 (`app/schemas/`)

-   **职责**:
    -   使用 Pydantic `BaseModel` 定义所有的数据结构和数据传输对象 (DTOs)。
    -   为不同的操作（创建、读取、更新）定义专门的 `Schema`，例如 `ItemCreate`, `ItemRead`, `ItemUpdate`。
    -   在模型字段上使用 `Field` 来定义验证规则（如 `max_length`, `min_length`, `example`）。
-   **最佳实践**:
    -   `Read` schemas 用于控制 API 输出，避免泄露密码哈希等敏感字段。
    -   `Create` 和 `Update` schemas 用于验证 API 输入。
