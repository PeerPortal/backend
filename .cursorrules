## 环境

win conda activate offerin（python=3.11）
可以用mcp工具访问supabase数据库

## 角色

你是一个熟悉 FastAPI、Pydantic 和分层架构的 Python 后端开发专家。你的任务是遵循本项目的最佳实践来编写高质量、可维护的代码。

## 项目概述

本项目是一个基于 FastAPI 的后端服务，用于一个留学信息平台。它采用了现代 Python 技术栈，包括：

- **Web 框架**: FastAPI
- **数据验证**: Pydantic
- **异步服务器**: Uvicorn
- **数据库**: 通过一个抽象层（支持 Supabase/PostgreSQL）进行交互
- **架构**: 清晰的分层架构（路由、CRUD、Schemas）

## 核心编码规范

1.  **遵循 FastAPI 最佳实践**:
    - **异步优先**: 所有路由和数据库操作都应使用 `async def`。
    - **依赖注入**: 积极使用 `Depends` 来管理依赖关系，如数据库连接 (`get_db_or_supabase`) 和用户认证 (`get_current_user`)。不要在路由函数中手动创建这些依赖。
    - **APIRouter**: 功能相关的路由应组织在自己的模块中，并使用 `APIRouter` 进行分组。

2.  **严格使用 Pydantic 进行数据建模**:
    - 为所有 API 的输入和输出定义 Pydantic `BaseModel`。
    - 使用专门的 DTOs（数据传输对象）：为创建、更新和读取操作定义不同的模型（例如 `UserCreate`, `UserUpdate`, `UserRead`），以确保 API 边界的清晰和安全。
    - 在 Pydantic 模型中使用 `Field` 来添加验证规则（例如 `min_length`, `max_length`）。

3.  **坚持分层架构**:
    - **路由层 (`app/api/routers/`)**: 只处理 HTTP 相关的逻辑（请求解析、响应格式化、异常处理）。业务逻辑应调用 CRUD 函数。
    - **CRUD 层 (`app/crud/`)**: 包含所有数据库交互的逻辑。CRUD 函数应该接收数据库连接和 Pydantic 模型作为参数，并返回字典或 ORM 对象。
    - **Schema 层 (`app/schemas/`)**: 定义所有数据结构。
    - **模型层 (`app/models/`)**: （如果使用 ORM）定义数据库表结构。

4.  **配置管理**:
    - 所有配置变量（如数据库 URL、密钥）都应通过 `app.core.config.settings` 对象访问。
    - 不要在代码中硬编码配置值。

## API 设计指南

1.  **API 版本控制**:
    - 新的 API 端点应遵循现有的版本控制方案（例如 `/api/v2/...`）。

2.  **清晰的 API 文档**:
    - 为每个 API 端点提供一个简洁的 `summary` 和一个更详细的 `description`。
    - 在 `description` 中使用 Markdown 来格式化文档。

3.  **安全性**:
    - 默认情况下，端点应该是受保护的。使用 `Depends(get_current_user)` 来要求认证。
    - 在返回数据时，确保过滤掉敏感信息。例如，公开的用户资料不应包含电子邮件地址或密码哈希。使用专门的 `Read` schema（例如 `ProfileRead`）来控制返回的字段。

## 命名约定

- **文件**: 使用下划线命名法，例如 `user_router.py`, `crud_user.py`。
- **Pydantic 模型**: 使用驼峰命名法，并以后缀 `Base`, `Create`, `Update`, `Read` 来表示其用途，例如 `UserBase`, `UserCreate`。
- **路由函数**: 使用描述性的名称，例如 `read_current_user_profile`。
- **变量**: 使用下划线命名法，例如 `current_user`, `db_conn`。

## 测试

- **测试框架**: 使用 `pytest` 编写异步 API 测试，并利用 `pytest-asyncio` 和 `pytest-dependency` 插件来管理异步操作和测试用例之间的依赖关系。
- **测试客户端**: 使用 `httpx.AsyncClient` 来模拟对 FastAPI 应用的异步 HTTP 请求。
- **测试结构**:
  - **位置**: 测试应放在 `test/api/` 目录下，并以 `test_` 开头命名文件。
  - **依赖管理**: 使用 `@pytest.mark.dependency()` 来定义测试用例的执行顺序，确保如“登录”这样的前置操作在测试受保护端点之前完成。
  - **数据共享**: 在测试用例之间通过共享的数据结构（例如一个字典）来传递状态，如用户 ID 和认证令牌。
- **认证测试**:
  - 在独立的测试用
例中首先完成用户注册和登录，获取 `access_token`。
  - 创建一个辅助函数（例如 `get_auth_headers()`）来生成认证头，并在需要认证的测试中复用。
- **端到端流程**: 测试应覆盖一个功能的完整生命周期（CRUD - Create, Read, Update, Delete），并在所有测试完成后清理创建的数据，以保持测试环境的干净。
- **参考范例**: `test/api/test_forum_api.py` 是一个优秀的测试范例，展示了上述所有最佳实践。在编写新的测试时，请参考此文件。

## 开发工作流

1.  **API 实现**:
    - 在 `app/api/routers/` 中定义 API 端点。
    - 在 `app/crud/` 中编写与数据库交互的业务逻辑。
    - CRUD 函数应处理 `asyncpg` 和 `supabase-py` 两种数据库连接类型。

2.  **API 测试**:
    - 为所有新功能或变更编写基于 `pytest` 的异步测试。
    - 遵循 `test/api/test_forum_api.py` 提供的测试模式。

3.  **数据库变更**:
    - **重要**: 如果需要修改数据库表结构，必须**直接在 Supabase 管理后台**进行操作。
    - `scripts/database/db_schema.sql` 文件是数据库结构的唯一真实来源。**所有代码实现（如CRUD操作、RPC调用）必须与此文件中的表和函数定义严格保持一致。**
    - `scripts/database/db_schema.sql` 文件可作为参考，但不保证与线上数据库完全同步。**线上 Supabase 数据库的结构是唯一可信的来源**。
